# 倒立振り子シミュレーター 方針書

## 目的

Arduinoで制御する倒立振り子のゲイン調整をシミュレーション上で行い、実機でのトライ&エラーを削減する。
実装言語: **Python**

---

## システム構成

### 運動モデル

- **運動平面**: 2次元平面（x方向の並進 + 傾斜角θ）
- **構成**: モーターに接続されたタイヤが回転して移動する倒立振り子。モーターは機体に固定。

### 機体パラメータ（要同定）

| パラメータ | 説明 |
|---|---|
| `M` | 機体全体の質量 [kg] |
| `L` | タイヤ接地点から重心までの距離 [m] |
| `(cx, cy)` | 機体座標系における重心位置 [m] |
| `I_body` | 機体の慣性モーメント [kg·m²] |

### タイヤ・モーターパラメータ（要同定）

| パラメータ | 説明 |
|---|---|
| `r` | タイヤ半径 [m] |
| `mu_s` | 静止摩擦係数 |
| `mu_k` | 動摩擦係数 |
| `I_wheel` | タイヤの慣性モーメント [kg·m²]（小さいと仮定：入力オフで即停止） |
| `K_motor` | モーター電圧→トルク変換ゲイン |

---

## センサー・アクチュエーター

### センサー

| センサー | 取得量 | 制御周期（デフォルト） |
|---|---|---|
| IMU | 機体傾斜角 θ、角速度 ω | 10 ms（設定可） |
| 磁気エンコーダー | タイヤ角度 → 角速度・角加速度を数値微分 | 10 ms（設定可） |

> シミュレーターでは `imu_dt` と `encoder_dt` を独立して設定できるようにする。

### アクチュエーター

- モータードライバーが PWM 信号を受け取り、モーターへの印加電圧を制御する
- PWM → 電圧 → トルク の変換をモデル化する

---

## 制御方式：カスケードループ PID

### 安定点の定義

- 機体の重心がタイヤ接地点の真上にある位置 → 傾斜角 θ = 0
- シミュレーション開始時はこの安定点に立った状態。外乱を与えて倒れるきっかけを作る。

### 外側ループ（傾斜角制御）

```
目標傾斜角: θ_ref = 0
入力: θ（IMUから取得）
誤差: Δθ = θ_ref - θ
PID出力: タイヤ目標角加速度 α_ref [rad/s²]
```

> **備考**: 目標量を角速度にするとモーターが追従できなかった実績あり。
> 角加速度指令と角速度指令の比較実験をシミュレーターで再現する価値がある。

### 外側→内側ループの接続（積分）

```
ω_ref += α_ref × dt   # 外側の角加速度指令を積分して角速度指令に変換
```

### 内側ループ（角速度制御）

```
目標角速度: ω_ref（外側ループ出力を積分したもの）
入力: ω_meas（エンコーダーの角度を1階数値微分）
誤差: Δω = ω_ref - ω_meas
PI出力: PWM値（→ 電圧 → モータートルク）
```

> **備考**: 内側ループはPIが実用的（Iゲインで定常速度偏差を除去できる）。
> 角加速度を目標にするとエンコーダーの2階微分が必要になりノイズが大きく不向き。
> モーターは本質的に「電圧→角速度」に近い特性（back-EMF）なので速度制御と相性が良い。

---

## 現状の問題点と考察

### 症状

機体が倒れてしまい制御が成立しない。原因として以下が疑われる：

| 候補 | メカニズム |
|---|---|
| 質量過大 | 重力トルク `M·g·L·sin(θ)` が大きく、モーターが追いつけない |
| 摩擦過大（静止摩擦） | 小さなPWM指令ではタイヤが動き出せないデッドゾーンが存在する |
| モータートルク不足 | 最大PWMでもタイヤに伝わるトルクが重力に負ける |

### 摩擦支配状態とIゲインの関係

静止摩擦が支配的な場合、角速度制御のIゲインを大きくすることで追従性が改善するという実測結果がある。これは次のように解釈できる：

- 静止摩擦にはデッドゾーンがある → 小さなPWMでは全く動かない
- Iゲインを大きくすると、速度誤差が蓄積されるにつれてPWM出力が急激に増大する
- 結果として静止摩擦を突き破れる大きなPWMバーストが発生する
- これは実質的に「角加速度制御」に近い挙動（大きなIゲイン → 誤差積分 ≈ 加速度指令）

```
大きなKᵢでの内側ループ挙動:
  PWM ≈ Kᵢ × ∫Δω dt  ← Δωが一定なら PWM が線形増加 → 加速度的
```

### シミュレーターで検証すべき仮説

1. **静止摩擦デッドゾーンのモデル化**: `|τ| < τ_static` のとき車輪が動かない状態を再現し、Iゲインとの相互作用を確認する
2. **トルク余裕の確認**: `τ_max（モーター最大） vs M·g·L` の比較で制御可能かどうかをシミュレーターで判定する
3. **Iゲインと静止摩擦の関係確認**: 大きなIゲインで静止摩擦デッドゾーンを突き破れるかをシミュレーターで定量的に確認する（摩擦補償は行わず、線形PIDの範囲で対処する）

---

## 物理モデル（運動方程式）

倒立振り子の非線形運動方程式をベースにする。

```
θ̈ = (M·g·L·sin(θ) - τ_wheel) / I_total
x̄  = r · φ_wheel   （スリップなし仮定 → 要摩擦判定）
```

- `τ_wheel`: タイヤから機体に伝わるトルク
- `I_total`: 機体 + タイヤの等価慣性モーメント
- スリップ判定: タイヤ接地力 < 摩擦力閾値のとき動摩擦係数を使用

---

## シミュレーター実装方針

### ファイル構成（案）

```
swing/
├── direction.md          # 本ファイル
├── sim/
│   ├── plant.py          # 物理モデル（運動方程式の積分）
│   ├── sensors.py        # IMU・エンコーダーのサンプリング・ノイズモデル
│   ├── controller.py     # カスケードPID
│   ├── motor.py          # PWM→電圧→トルク変換
│   └── main.py           # シミュレーション実行・プロット
└── params.py             # 全パラメータ一元管理
```

### 数値積分

- 固定ステップ RK4 で物理を更新（`dt = 1ms` 程度）
- 制御ループは `imu_dt` / `encoder_dt` ごとに離散的に呼び出す（サンプルホールド）

### 出力・可視化

- 時系列プロット: θ, ω_wheel, ω_ref, PWM
- パラメータ変更 → 再実行で安定性を比較できること
- 将来的に実機ログと重ねて比較できるフォーマットにする

---

## Python実装ステップ

### Step 1: `params.py` — パラメータ定義

全物理・制御パラメータをdataclassで一元管理する。

```python
# 最低限定義するパラメータ
M, L, I_body       # 機体（質量・重心距離・慣性モーメント）
r, I_wheel         # タイヤ（半径・慣性モーメント）
mu_s, mu_k         # 摩擦係数
K_motor, pwm_max   # モーター特性
imu_dt, encoder_dt # センサー周期
```

### Step 2: `plant.py` — 物理モデル

倒立振り子の運動方程式を実装する。

```python
# 状態変数: [θ, θ̇, φ_wheel, φ̇_wheel]
def derivatives(state, tau_motor, params): ...

# 摩擦モデル（最重要）
def friction_torque(omega_wheel, tau_applied, params):
    # |tau| < tau_static → 静止（デッドゾーン）
    # それ以外 → 動摩擦
```

摩擦デッドゾーンを正しくモデル化することで「Iゲイン大 → 突破」の挙動を再現できる。

### Step 3: `motor.py` — PWM→トルク変換

```python
def pwm_to_torque(pwm, omega_wheel, params):
    voltage = pwm / 255 * V_supply
    # back-EMF考慮: τ = K_t * (V - K_e * ω) / R
```

### Step 4: `sensors.py` — センサーモデル

```python
class IMU:
    # imu_dt ごとにサンプリング（ゼロ次ホールド）
    # ガウスノイズ付加（オプション）

class Encoder:
    # encoder_dt ごとにサンプリング
    # 角速度 = Δφ / encoder_dt（1階差分）
```

### Step 5: `controller.py` — カスケードPID

```python
class OuterLoop:
    # 入力: Δθ、出力: α_ref
    # PID（Kp, Ki, Kd）

class InnerLoop:
    # 入力: Δω、出力: PWM
    # PI（Kp, Ki）
    # ω_ref は α_ref を積分して生成

def cascade_step(theta, omega_wheel, outer, inner, dt): ...
```

### Step 6: `main.py` — シミュレーション実行

```python
# 1. 初期状態: θ=0（直立）、小さな外乱を与える
# 2. ループ:
#    a. RK4で物理を dt=1ms 更新
#    b. imu_dt ごとに外側ループ更新
#    c. encoder_dt ごとに内側ループ更新
#    d. ログ記録
# 3. matplotlib でプロット
```

### Step 7: 検証実験

| 実験 | 確認内容 |
|---|---|
| `mu_s` を変化させる | デッドゾーンの影響とIゲインの必要量 |
| `K_motor` を変化させる | トルク余裕の確認（倒れるかどうかの閾値） |
| 外側ループ: α_ref指令 vs ω_ref直接指令 | 追従性の比較（実機での実績と照合）|
